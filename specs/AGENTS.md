# Specs Guidance

This file contains working rules for maintaining the spec corpus in `specs/`.

The canonical governance source is `specs/README.md`.

Use `specs/README.md` for authority levels, support-status labels, shared
vocabulary, cross-spec invariants, and major-spec update rules.
